[project]
name = "pageindex-mcp"
version = "1.0.0"
description = "PageIndex MCP - High-accuracy RAG without vector database"
dependencies = ["mcp>=1.0.0", "openai>=1.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
